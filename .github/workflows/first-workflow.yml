name: My First Workflow

on: push

jobs:
  first job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome Message
        run: echo "Welcome to CTL"

      - name: list files
        run: ls

      - name: Read file
        run: cat README.md