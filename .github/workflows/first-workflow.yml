name: My First Workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v6
        
      - name: Welcome Message
        run: echo "Welcome to CTL"

      - name: list files
        run: ls

      - name: Read file
        run: cat README.md